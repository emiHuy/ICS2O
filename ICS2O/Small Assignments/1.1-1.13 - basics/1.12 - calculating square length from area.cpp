//This program calculated the length of a square tile if the area is given
#include <iostream>
#include <cmath>
using namespace std;
int main() {
  cout << "If a square tile has an area of 81 square meters, then the length of the side of the tile is " << sqrt (81) << " meters.";
  return 0;
}